# Benchmark — Hy-MT2-1.8B 翻译基准（2026-09-04）

> 复现：`node benchmarks/bench.mjs [url] [repeat]`（零依赖；请求体与扩展逐字一致：`model=qwen3-translate`(alias)、`temperature=0.0`、`max_tokens=256`、`stream=false`、`chat_template_kwargs.enable_thinking=false`、单条 user 消息 `Translate to Simplified Chinese:\n<line>`）。

## 环境

| 项 | 值 |
|---|---|
| 机器 | Apple M4 Pro / 48GB / 12 核（Mac16,8, arm64） |
| 模型 | Hy-MT2-1.8B-Q4_K_M（`~/hy-mt2-1.8b-q4km.gguf`, 1,133,080,448 B, 2026-09-04） |
| 服务 | llama.cpp b10796 llama-server, 127.0.0.1:9911, `--parallel 2 --ctx-size 4096 --threads 4 --cont-batching --no-warmup --log-disable` |
| 并发 | 单并发（扩展行为：行级串行/双槽排队） |

## 结果（2026-09-04T13:32 UTC 实测）

| 用例 | 延迟 | 结果 |
|---|---|---|
| short（短句） | 108 ms | 用户询问当前修复是否正确。 |
| medium | 125 ms | 让我检查一下交互模式与会话事件发射器之间的处理顺序。 |
| code-tokens（`` `src/server.ts` ``, `npm test`） | 159 ms | ✅ 保留 `src/server.ts` / `npm test` 原样 |
| url | 165 ms | ✅ 保留 `https://github.com/earendil-works` 原样 |
| question | 106 ms | 如果翻译服务器在流过程中崩溃会怎么样？ |
| sequence | 152 ms | 首先，读取文件。其次，检查健康端点。然后，重新启动服务。 |
| long（~100 词） | 663 ms | 完整翻译（含 `renderLayoutFrame` 保留） |
| very-long（~330 词探测） | 1533 ms | ⚠️ 超预算截断（见下） |
| mixed-metrics | 173 ms | 68.8% / 29 毫秒 数字语义保留 |
| markdown（加粗/-） | 149 ms | **重要：** 格式与破折号保留 |
| bullet-reasoning（冒号结构） | 148 ms | 根本原因/后果 结构保留 |

### 汇总

| 指标 | 值 |
|---|---|
| 成功 / 失败 | 11 / 0（空响应 0） |
| 代码 / 路径 / URL token 保留 | 4/4 = 100% |
| 延迟 p50 / p95 / max | 152 / 1533 / 1533 ms |
| 延迟 min | 106 ms |

## 截断边界探测（重要）

- 输入 ~100 词：完整翻译，无截断。
- 输入 ~330 词（≈399 prompt tokens）：`finish_reason=length`，`completion_tokens=256`（预算打满），译文在句中截断 —— **单行 >~250 词时有截断风险**。

> 结论：`max_tokens=256` 对 typical thinking 行（≤100 词）安全；超长行截断为已知限制（扩展目前不做分行/分块），阅读时留意"译文戛然而止"即超长行。

## 与流式显示的配合

- 服务空闲 CPU ≈0.1%（无任务风暴，对照历史 CPU 风暴事故 ADR-0002）。
- 扩展内逐行请求 ≈ 上述单行延迟 × 行数；因 `--parallel 2` 双槽 + 串行去重，实际拖尾 = 最长行延迟（典型 <1s）。

---

## 真实语料质量回归（2026-09-04，追加）

> 语料：`benchmarks/corpus/thinking-corpus.json` —— 16 条**真实 pi assistant-thinking** 样本（从全部历史会话自动收割分层采样），覆盖 code/url/num/path/question/long/plain 7 类。
> 工具：`node benchmarks/test-corpus.mjs`（零依赖；请求体与扩展逐字一致）。

### 结果：16/16 PASS

| 指标 | 值 |
|---|---|
| 通过 | **16 / 16**（修正断言后；另 1 条非英文输入按扩展真实行为 SKIP） |
| 延迟 p50 / p95 / max | 829 / 1726 / 1726 ms |
| 截断（finish=length） | 0 |
| 中文占比（prose 类） | 0.61–0.90 |
| 数字/URL/路径/标识符保留 | 100%（断言修正后） |

### 断言设计要点（防误报，对照 method/falsifiable-checks）
- 数字断言只查 **≥2 位连续数字串**（十六进制 `0x5db2`、`152/152` 这类不会被拆散误报；`0 events → 没有事件` 属合法意译不算丢）。
- code/url 类中文占比阈值放宽（专名/代码拉低比例，不代表质量差）。
- 中文输入（isTranslatable 会跳过）直接 SKIP 不算 FAIL。
- 真实踩坑：初版断言把 4 个实质正确的译文判 FAIL（hex 拆数字、中文原样保留、`0→没有` 意译）——全部为断言误报，修正后全绿。

### 一个真实鲁棒性发现
thinking 里偶尔混入 **未闭合的 DSML/伪 XML 标签残留**（模型采样产物）。扩展逐行翻译对这类行不会崩（isTranslatable 英文门槛拦截或原样透传），语料里原坏样例已剔除（不是扩展问题，是语料质量问题）。

---

## 量化档位对比：Q4_K_M vs 官方 2Bit（2026-09-04 深夜，追加）

> 用同一真实语料（16 例 thinking-corpus.json）同机对比。官方 2Bit 文件 sha256 校验通过（`dcc33bba…`）。
> ⚠️ 过程教训：社区量化（mradermacher Q3_K_M/Q2_K）与本机 llama.cpp 不兼容/损坏（Q3 全截断、Q2 输出乱码），且 **curl `-C -` 续传 + HF xet CDN 会产生 sha256 不匹配的坏文件** —— 必须下载后校验 sha256。社区文件已弃用删除。

| 模型 | 文件 | RSS | p50 | p95 | 语料 PASS | 难例质量 |
|---|---|---|---|---|---|---|
| **Hy-MT2 Q4_K_M（现产）** | 1.06 GB | ~2.2GB | 829ms | 1726ms | 16/16 | hex/路径/URL 全保留 |
| **Hy-MT2 官方 2Bit** | **0.56 GB** | 2.18GB | **582ms** | 1765ms | **16/16** | hex/路径/API key/URL 全保留 ✅ |

### 结论
- **官方 2Bit（0.56GB）质量与 Q4_K_M 相当**：16/16 语料 PASS，hex/路径/API key/URL 全保留，语义准确，速度还略快（模型更小、带宽压力更低）。
- **内存几乎没省**：RSS 都是 ~2.2GB —— 因为 1.8B 小模型的内存大头是 **Metal 运行时 + KV cache + 固定开销**（~1.6GB），不是权重。文件省 0.5GB 反映不到 RSS 上。
- **建议**：保持 Q4_K_M（质量余量更大、社区验证多）；若想省磁盘 0.5GB 或换更小模型，官方 2Bit 是安全替代。**不值得为省内存换 2Bit**——省不到内存。

### 传输教训（重要）
- HF 大文件下载必须：认证 token + **下载后校验 sha256**（curl 续传在 xet CDN 上会产生坏字节）。
- 社区量化仓库（mradermacher 等）文件可能与官方架构/llama.cpp 版本不兼容，优先官方（tencent/Hy-MT2-1.8B-2Bit-GGUF）。
